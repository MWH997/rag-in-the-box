{
  "name": "@rag/api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "node ../../scripts/dev-preflight.mjs ${API_PORT:-8787} API && wrangler dev --local --port ${API_PORT:-8787} --var BETTER_AUTH_URL:http://localhost:${API_PORT:-8787} --var ALLOWED_ORIGIN:http://localhost:${WEB_PORT:-5173}",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply rag-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply rag-db --remote",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.7.2",
    "@rag/shared": "*",
    "better-auth": "^1.7.2",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.27",
    "mammoth": "^1.12.0",
    "papaparse": "^5.5.4",
    "unpdf": "^1.6.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "@types/papaparse": "^5.5.2",
    "drizzle-kit": "^0.31.10",
    "vitest": "^4.1.9",
    "wrangler": "^4.127.1"
  }
}
